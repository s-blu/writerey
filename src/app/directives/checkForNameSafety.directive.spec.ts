/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CheckForNameSafetyDirective } from './checkForNameSafety.directive';

describe('Directive: CheckForNameSafety', () => {
  it('should create an instance', () => {
    const directive = new CheckForNameSafetyDirective();
    expect(directive).toBeTruthy();
  });
});
